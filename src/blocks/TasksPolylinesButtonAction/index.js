import styled from 'styled-components'

import {
  Button,
  Label
} from '../../elements'

const TasksPolylinesButtonAction = styled(Button)`
  width: 24px;
  marginLeft: 1px;
`

TasksPolylinesButtonAction.Label = Label

export default TasksPolylinesButtonAction
